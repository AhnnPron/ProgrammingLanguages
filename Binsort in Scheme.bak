;; The first three lines of this file were inserted by DrRacket. They record metadata
;; about the language level of this file in a form that our tools can easily process.
#reader(lib "htdp-advanced-reader.ss" "lang")((modname |Binsort in Scheme|) (read-case-sensitive #t) (teachpacks ()) (htdp-settings #(#t constructor repeating-decimal #t #t none #f () #f)))
(define bintree
  (lambda (leaf interior-node)))

(define leaf
  (lambda (lst)
    (if (null? lst)
        '()
        (cons(car lst)(leaf(cdr lst))))
        
(define interior-node
  (lambda (lst)
    (if (null? lst)
        '()
        (cons(car lst)(interior-node(cdr lst))))

(define leaf?
  (lambda (lst n)
    (if (null?

       
  
  